Workspace and Artifacts (verified)

Confirmed layout sources: `docs/manual.md`, `finsec/config/workspace.py`.

Important files and roles:
- `workspaces/<slug>/target.yaml` — target spec, scope, accounts, safety policy (generated by `hunt setup`).
- `workspaces/<slug>/observations/` — redacted observations and normalized stores (generated by ingestion commands).
- `workspaces/<slug>/model/` — derived model docs (`architecture.md`, `workflows.md`, `invariants.yaml`).
- `workspaces/<slug>/behavior/` — actions, resource instances, workflow instances/families,
  states, transitions, propagation links, and deterministic graph JSON.
- `workspaces/<slug>/model/business-invariants.yaml` — inferred `BINV-*` rules.
- `workspaces/<slug>/hypotheses/backlog.yaml` — canonical `HYP-*` and synchronized `BLH-*` backlog.
- `workspaces/<slug>/hypotheses/business-logic.yaml` — full explainable logic-hypothesis records.
- `workspaces/<slug>/tests/plans/plans.yaml` — generated plans with embedded checksum-bound
  approval metadata when approval is granted.
- `workspaces/.finsec-secrets/` — isolated secret storage for captured credentials (0600/0700 protections).

Artifact properties:
- Generated vs editable: model and hypothesis artifacts are generated; human edits are preserved and conflict-tracked.
- Sensitive: observations and evidence may contain sensitive redacted artifacts; never print raw values or copy secrets.
